import cython

def hello_world():
    print("hello world")

def fib(n):
    """Print the Fibonacci series up to n."""
    a: cython.int
    b: cython.int

    a, b = 0, 1
    while b < n:
        print(b)
        a, b = b, a + b

    print()
